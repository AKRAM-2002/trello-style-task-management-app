import LoginForm from '@/components/LoginForm'; // Ensure this path is correct based on your file structure

export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background">
      <LoginForm />
    </div>
  );
}
